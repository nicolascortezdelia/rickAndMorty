import React from 'react';
import { Button } from '../../components/Button';
import { Header } from '../../components/Header';

const Home = () => {
  return (
    <section>
      <Header/>
      <Button></Button>
    </section>
  );
};

export default Home;
